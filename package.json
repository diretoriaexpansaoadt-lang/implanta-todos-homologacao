{
  "name": "implanta-todos-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Implanta TODOS, plataforma de implantacao da Audicao de TODOS.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js",
    "migrate:postgres": "node scripts/migrate-json-to-postgres.js",
    "alerts": "node scripts/run-alerts.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.828.0",
    "pg": "^8.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
